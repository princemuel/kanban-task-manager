import localFont from 'next/font/local';

const plus_jarkata_sans = localFont({
  src: './plus-jarkata-sans.ttf',
  variable: '--font-sans',
  display: 'swap',
});

export { plus_jarkata_sans as FontSans };
